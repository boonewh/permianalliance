# Normalize line endings: LF in the repo, native in the working tree.
* text=auto

# Files consumed by the Linux build must always be LF.
requirements.txt text eol=lf
vercel.json      text eol=lf
*.py             text eol=lf
*.html           text eol=lf
